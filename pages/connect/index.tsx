import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import { Box, Stack, TextField, Typography } from '@mui/material'
import styles from '../../styles/Connect.module.css'

const Connect: NextPage = () => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main className={styles.main}>
        <Box sx={{ p: 6, borderRadius: 2, backgroundColor: '#292933' }}>
          <Stack spacing={1} direction="column">
            <Typography variant="h4" color="#FFFFFF">{'MONGO ADMIN'}</Typography>
            <TextField variant="standard" label="HOST" InputLabelProps={{ style: { color: '#898989' } }} sx={{ input: { color: '#FFFFFF' } }} />
            <TextField variant="standard" label="USERNAME" InputLabelProps={{ style: { color: '#898989' } }} sx={{ input: { color: '#FFFFFF' } }} />
            <TextField variant="standard" type="password" label="PASSWORD" InputLabelProps={{ style: { color: '#898989' } }} sx={{ input: { color: '#FFFFFF' } }} />
          </Stack>
        </Box>
      </main>

      <footer className={styles.footer}>
        <a
          href="https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          Powered by{' '}
          <span className={styles.logo}>
            <Image src="/vercel.svg" alt="Vercel Logo" width={72} height={16} />
          </span>
        </a>
      </footer>
    </div>
  )
}

export default Connect
